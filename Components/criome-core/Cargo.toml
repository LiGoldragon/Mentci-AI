[package]
name = "criome-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
cozo = { version = "0.7", default-features = true }
rayon = "=1.5.3"

[dev-dependencies]
tempfile = "3"

[workspace]
