# Lane Namespace Mirroring

## Accepted Rule
Component repositories mirror top-level lane names to preserve alignment across master and component flows.

## Example
`mentci-ai:dev` aligns with `component:dev` lanes for governed synchronization.

## Impact
- predictable cross-repo lane routing,
- simpler policy expression for authority checks,
- reduced ambiguity in lane-target mapping.
