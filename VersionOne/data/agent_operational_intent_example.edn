{:version 1
 :message :AgentOperationalIntent

 ;; mentci-ai repo defaults (subject to override by lane governor)
 :primaryBookmark "dev"
 :primaryRemote "origin"
 :integrationBookmark "main"

 ;; policy: the human operator is the only actor that moves `primaryBookmark`
 :bookmarkMovesAllowedBy :user

 ;; required: end-of-turn push target
 :endOfTurnPushTarget {:remote "origin" :bookmark "dev"}

 ;; optional: revset fragments used for alignment verification
 :alignmentVerification
 {:local "bookmarks(dev)"
  :remote "remote_bookmarks(dev@origin)"}

 :notes "This object is intended to be produced by a lane governor repo/process and passed to agents as an init envelope."}
