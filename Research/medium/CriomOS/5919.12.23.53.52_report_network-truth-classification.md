# 5919.12.23.53.52 CriomOS ↔ Maisiliym Network Truth Classification

**MUST UPDATE WHEN EDITING THIS REPO:** Keep the documents operator-first, guidance-doc-primary, and happy-path-only. Update `/home/li/git/Mentci-AI--dev/Components/CriomOS/docs/GUIDELINES.md` (the canonical CriomOS node/network guidance), sync its mirror `/home/li/git/Mentci-AI--dev/Components/CriomOS/docs/AGENTS.md`, and record the new truth in `/home/li/git/maisiliym/docs/GUIDELINES.md` before any commit touching cluster or node metadata so the operator docs stay highest authority.

## Valid progress (Guidance doc primary)
1. `/home/li/git/Mentci-AI--dev/Components/CriomOS/nix/mkCrioZones/mkHorizonModule.nix` keeps node construction purely downstream. The horizon object derives `nodeCriomeDomainName`, `node`, `exNodes`, and `cluster` fields from `Clusters.${clusterName}` (fed by `/home/li/git/maisiliym/datom.nix`), so CriomOS merely consumes the Maisiliym-supplied truth instead of re-authoring it.
2. `/home/li/git/Mentci-AI--dev/Components/CriomOS/nix/mkCriomOS/network/default.nix` imports `./unbound.nix`/`./yggdrasil.nix` and folds `exNodes` through `mkCriomeHostEntries`. The resulting `networking.hosts` attrset and `nameservers` list are happy-path maps from canonical node data to the deployed host tables.
3. `/home/li/git/Mentci-AI--dev/Components/CriomOS/nix/mkCriomOS/network/unbound.nix` derives `tailnetBaseDomain` from `horizon.cluster.name`, gates the split-DNS `forward-zone` on the horizon-driven `isTailnetNode` predicate, forwards `tailnet.${cluster.name}.criome.` to Headscale at `100.100.100.100`, and keeps the root zone on the TLS upstream servers defined in `TLSDNServers`. This dynamic-unbound approach keeps DNS semantics reactive to Maisiliym truth instead of hard-coded literals, preserving CriomOS as a consumer of the network data.

## Invalid data-authority drift (Maisiliym owns node/network truth, CriomOS consumes/builds/deploys it)
1. `/home/li/git/Mentci-AI--dev/Components/CriomOS/nix/homeModule/min/default.nix` now creates a `prometheus-deepseek-70b` user service that hard-codes `/home/li/.local/share/prometheus-llama/models/DeepSeek...` and `pkgs.llama-cpp-rocm`. The `prometheus` node identity (species largeAI, `trust = 3`, `nodeIp = "5::5/128"`, and the `yggdrasil` keys) lives solely in `/home/li/git/maisiliym/datom.nix`, so any change that shifts the service footprint of that node must be recorded there first and propagate through the horizon pipeline. Otherwise the operator docs note conflicting truths.
2. The `litellmRouterYaml` map introduced `deepseek-r1-distill-llama-70b` as a `model_group_alias`, mirroring the `prometheus` node alias without a Maisiliym-sourced anchor. Operators must capture such alias or DNS frame truth in `/home/li/git/Mentci-AI--dev/Components/CriomOS/docs/GUIDELINES.md` (and, when Maisiliym truth changes, the `/home/li/git/maisiliym/docs/GUIDELINES.md` `NodeProposal.nodes.*` entry) before touching the CriomOS consumer modules so the network plan stays centralized.

## Operator notes (happy path)
- Always read `/home/li/git/maisiliym/docs/GUIDELINES.md` (the canonical Maisiliym node/network guidance) before touching node or network metadata. It reminds operators that every truth update begins inside `datom.nix` and the `NodeProposal.nodes.*` map.
- When you work inside CriomOS, update `/home/li/git/Mentci-AI--dev/Components/CriomOS/docs/GUIDELINES.md` (the primary operator guidance) and keep `/home/li/git/Mentci-AI--dev/Components/CriomOS/docs/AGENTS.md` in sync; these guidance docs are the happy-path authority, not the implementation files themselves.
- The dynamic-unbound approach in `/home/li/git/Mentci-AI--dev/Components/CriomOS/nix/mkCriomOS/network/unbound.nix` derives `tailnetBaseDomain` from `horizon.cluster.name`, gates the split DNS zone on the horizon-driven `isTailnetNode`, forwards `tailnet.${cluster.name}.criome.` to Headscale at `100.100.100.100`, and keeps the root zone on the TLS upstreams defined in `TLSDNServers`. Do not hard-code DNS records anywhere; extend the horizon export payload so the new truth flows into unbound, DHCP, and kube provisioning automatically.
- Remember the architectural rule: Maisiliym owns node/network truth and CriomOS consumes/builds/deploys it. Any change must flow doc → `datom.nix`/`NodeProposal` → horizon export → CriomOS consumers so operators never carry conflicting truths.
