/target
result
.jj/
.git/
.direnv/
*.swp
/inputs/
/workspace/
/$(pwd)/
gemini-cli-*.tgz
*.tgz
# Sources are mounted/ephemeral read-only substrate (often symlinks/store paths).
# They are environment material, not repository source of truth.
Sources/
inputs_backup/
# Outputs are runtime/session artifacts and must not be versioned.
Outputs/
/.mentci/
/.gemini/